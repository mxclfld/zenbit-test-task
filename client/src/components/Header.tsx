import styled from 'styled-components';

const StyledHeader = styled.h1`
  margin-bottom: 1.875rem;
  line-height: 3.25rem;
  font-size: 2.5rem;
`;

const Header = () => {
  return <StyledHeader>Reach out to us!</StyledHeader>;
};

export default Header;
